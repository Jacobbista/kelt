apiVersion: v1
kind: Service
metadata:
  name: keycloak-db
  namespace: {{ iam_namespace }}
  labels:
    app: keycloak-db
spec:
  selector:
    app: keycloak-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      name: postgres
  type: ClusterIP
