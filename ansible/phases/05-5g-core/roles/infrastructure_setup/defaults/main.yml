---
# CRI endpoints (k3s uses its own containerd socket; edge uses standalone containerd)
worker_cri_endpoint: "unix:///run/k3s/containerd/containerd.sock"
edge_cri_endpoint: "unix:///run/containerd/containerd.sock"

# Storage class configuration
storage_class_name: "hostpath-storage"
storage_provisioner: "kubernetes.io/no-provisioner"

# NF images come from nf_images in all.yml; no legacy images needed.
container_images: []
