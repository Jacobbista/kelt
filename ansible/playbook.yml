---
# LEGACY PLAYBOOK - DEPRECATED
# This playbook has been replaced by a phase-based structure.
# Please use instead: ansible-playbook phases/00-main-playbook.yml

- name: "⚠️  DEPRECATED PLAYBOOK"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Display deprecation notice"
      debug:
        msg: |
          ⚠️  THIS PLAYBOOK IS DEPRECATED
          =====================================

          The project has been reorganized into phases for more controlled deployment.

          🚀 NEW COMMAND:
          ansible-playbook phases/00-main-playbook.yml

          📁 ORGANIZED STRUCTURE:
          phases/01-infrastructure/     - Basic infrastructure setup
          phases/02-kubernetes/         - Kubernetes cluster (k3s)
          phases/03-kubeedge/           - KubeEdge integration
          phases/04-overlay-network/    - Overlay network (OVS + Multus)
          phases/05-5g-core/            - 5G Core Network Functions
          phases/06-ueransim-mec/       - UERANSIM and MEC

          📖 For more details: cat phases/README.md
